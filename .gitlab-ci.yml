image: docker:latest
 
services:
  - docker:dind
 
stages:
  - build
  # - test
  - deploy

before_script:
  - docker info

build:
  stage: build
  script:  
    - docker-compose build
    - docker-compose images

deploy:
  stage: deploy
  script:
    - docker-compose up -d
    - docker-compose images
    - docker-compose ps
    - docker-compose logs
    - docker ps
    - docker ps -a
