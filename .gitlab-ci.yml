image: docker:20.10.16

services:
  - docker:20.10.16-dind

variables:
  DOCKER_DRIVER: overlay2

stages:
  - build
  - deploy

before_script:
  - docker info

build:
  stage: build
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker rmi projet-jpo || true
    - echo "Construction de l'image projet-jpo en cours..."
    - docker build -t projet-jpo .
    - docker tag projet-jpo:latest registry.gitlab.com/mouadagn/projet-jpo:latest
    - docker push registry.gitlab.com/mouadagn/projet-jpo:latest
  artifacts:
    paths:
      - /app/logs/build.log

deploy:
  stage: deploy
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker stop projet-jpo || true
    - docker rm projet-jpo || true
    - echo "Démarrage du conteneur projet-jpo en cours..."
    - docker run -d -p 80:80 registry.gitlab.com/mouadagn/projet-jpo:latest
  artifacts:
    paths:
      - /app/logs/deploy.log
