image: docker:latest
 
services:
  - docker:dind
 
stages:
  - build
  - deploy

before_script:
  - docker info

build:
  stage: build
  script:  
    - docker-compose images

deploy:
  stage: deploy
  script:
    - bash deploy.sh
    - docker-compose images
    - docker-compose ps
    - docker-compose logs
    - docker ps
    - docker ps -a
